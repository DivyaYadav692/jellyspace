version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - echo Installing dependencies...
      - cd jellyspace  # Navigate to the jellyspace directory
      - npm install    # Run npm install inside jellyspace
  build:
    commands:
      - echo Building the application...
      - cd jellyspace  # Ensure you're still in the jellyspace directory
      - npm run build  # Run the build command inside jellyspace

artifacts:
  files:
    - jellyspace/build/**/*  # Assuming the build output is in the build folder inside jellyspace
